#pragma once

// Dynamic Programming Solver
struct DpSolver {
    static bool isMatch(const char *s, const char *p) {
        // This is not a real implementation, just a stub.
        return true;
    }
};